package h2;

public class H2_main {
    public static void main(String[] args) {

        int n = 0, digits = 0;
        int[] a = new int[9];   

        if (n == 0) {
            digits = 1;
            a[8] = 0;   
        } else {
            int temp = n;
            while (temp > 0) {
                digits++;
                temp /= 10;
            }
            int index = 8;    
            temp = n;

            while (temp > 0) {
                a[index] = temp % 10;  // letzte Ziffer einfügen
                temp /= 10;            // entferne letzte Ziffer
                index--;               // gehe eine Zelle nach links
            }
        }

        // Ausgabe zu Kontrollzwecken
        System.out.println("digits = " + digits);
        System.out.print("Array a = ");
        for (int value : a) {
            System.out.print(value + " ");
        }
    }
}
